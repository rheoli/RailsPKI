<html>
<head>
<style type="text/css">
.title { font-family: Arial, Helvetica, sans-serif; font-weight: bold; font-size: 16px }
.text {
        FONT-WEIGHT: plain;
        FONT-SIZE: 12px;
        COLOR: #000;
        FONT-STYLE: normal;
        FONT-FAMILY: Arial, Helvetica, sans-serif;
        FONT-VARIANT: normal;
        TEXT-DECORATION: none;
}
.text a {
        FONT-WEIGHT: plain;
        FONT-SIZE: 12px;
        COLOR: #c00;
        FONT-STYLE: normal;
        FONT-FAMILY: Arial, Helvetica, sans-serif;
        FONT-VARIANT: normal;
}
.text a:hover {
        COLOR: #c00;
        TEXT-DECORATION: underline;
}
.text a:visited {
        COLOR: #933;
}

</style>
</head>
<body>
<%
 aLevel=["Extremly critical", "Highly critical", "Moderately critical", "Less critical", "Not critical"]
%>
<h3 class="title">Secunia Meldungen</h3>
<% aLevel.each do |sType| %>
<%   next if !@info.has_key?(sType) %>
 <h4 class="text"><b><%= sType %></b></h4>
 <ul>
 <% @info[sType].each do |hMsg| %>
  <li><a class="text" href="http://<%= hMsg["id"] %>"><%= hMsg["title"] %></a></li>
 <% end %>
 </ul>
<% end %>
</body>
</html>
